{
  "name": "@eagleeye/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Motor de inferencia en navegador (slice vertical): camara -> MediaPipe Face Landmarker -> overlay + HUD de FPS/latencia.",
  "scripts": {
    "test": "node --test",
    "vendor": "node scripts/vendor.js",
    "dev": "node scripts/serve.js"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.35"
  },
  "devDependencies": {
    "@eagleeye/protocol": "workspace:*"
  }
}
